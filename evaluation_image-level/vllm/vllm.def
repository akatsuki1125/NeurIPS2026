Bootstrap: docker
From: vllm/vllm-openai:v0.19.1

%setup
    echo "Start building sif!"

%files
    ./evaluation_image-based/vllm/entrypoint.sh /opt/setup_dir/entrypoint.sh

%post
    chmod +x /opt/setup_dir/entrypoint.sh

%runscript
    exec /opt/setup_dir/entrypoint.sh "$@"
